#ifndef MACROLIKE_H
#define MACROLIKE_H
/**
 * 3-function_like_maacro: is the file
 * @x: the input value (an int)
 * Description: the header file that computes absolute value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
